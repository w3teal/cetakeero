name: cetakeero
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.4

dependencies:
  catppuccin_flutter: ^1.0.0
  desktop_drop: ^0.8.0
  file_picker: ^13.1.0
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  flutter_svg: ^2.3.0
  pdf: ^3.13.1
  printing: ^5.15.1
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2

dependency_overrides:
  printing:
    path: third_party/printing

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
